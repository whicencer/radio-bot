const { addSource } = require('./addSource');
const { addSourceYoutube } = require('./addSourceYoutube');
const { getCurrentSource } = require('./getCurrentSource');
const { removeSource } = require('./removeSource');

module.exports = {
	addSource,
	addSourceYoutube,
	getCurrentSource,
	removeSource
};